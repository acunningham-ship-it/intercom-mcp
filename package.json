{
  "name": "intercom-mcp",
  "version": "0.2.0",
  "description": "MCP server letting Claude Code sessions on the same box talk to each other (ask/answer/broadcast) via a shared SQLite message bus",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/two-agents.test.js && node test/noise-and-presence.test.js && node test/watch.test.js && node test/v2-watchers.test.js && node test/v3-features.test.js && node test/topic-watcher.test.js && node test/pid-reuse.test.js && node test/r2-a-claim-assign.test.js && node test/respond-collect.test.js && node test/smart-stale-ask.test.js && node test/monitor-watcher-args.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.4"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
