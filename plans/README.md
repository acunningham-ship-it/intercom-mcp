# Implementation Plans

Generated by the improve skill on 2026-06-17 (advisor pass over HEAD `53b4bd5`).
Execute in the order below unless dependencies say otherwise. Each executor: read
the plan fully before starting, honor its STOP conditions, and update your row
when done (unless a reviewer told you they maintain the index).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Wire the orphaned test file into `npm test` + add coverage for the gaps the other plans depend on | P1 | S | — | TODO |
| 002  | Extract the shared "unread for me" query (with topic routing) so watchers match the server | P1 | M | 001 | TODO |
| 003  | Make the `join` name-claim atomic (close the TOCTOU) | P2 | M | 001 | TODO |
| 004  | Harden presence against PID reuse | P2 | M | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **001 goes first** — it establishes the verification baseline (wires in the orphaned
  test file and adds tests that 002/003/004 rely on to prove they didn't regress).
- 002, 003, 004 each depend on 001 but are **independent of each other** and edit
  **disjoint regions of `server.js`** (002: `unreadFor`; 003: `registerAs`; 004:
  `pidAlive`), so after 001 lands they can run as parallel lanes and integrate cleanly.

## Findings considered and rejected

- **No authentication / trusts all local sessions** — by design (single-box, all local
  CLIs are trusted peers). Documented in README. Not a finding.
- **JS-layer topic filtering over-fetches rows then filters in memory** — negligible at
  this bus's scale (single box, retention-capped); not worth the SQL complexity.
- **`ask`/`inbox` poll with `sleep(400)`** — the no-daemon, no-port design's whole point;
  intentional.
- **`exit`-handler cleanup doesn't fire on SIGTERM/SIGINT in `server.js`** — benign: dead
  rows are pruned by `pidAlive` on the next `onlineAgents()` call, so presence
  self-heals. Not worth a plan (would only shave the self-heal latency).
